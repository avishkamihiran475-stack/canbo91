import { throttle } from './utils.js';
export function initNavbar(){const navbar=document.getElementById('navbar'); const toggle=document.getElementById('nav-toggle'); const overlay=document.getElementById('nav-overlay'); const closeBtn=document.getElementById('nav-close'); if(!navbar)return; function onScroll(){navbar.classList.toggle('scrolled',window.scrollY>100);} window.addEventListener('scroll',throttle(onScroll,50)); onScroll(); setActiveLinks(); if(toggle&&overlay){const setMenuOpen=(open)=>{toggle.classList.toggle('active',open); overlay.classList.toggle('active',open); toggle.setAttribute('aria-expanded',String(open)); document.body.style.overflow=open?'hidden':'';}; toggle.addEventListener('click',()=>setMenuOpen(!overlay.classList.contains('active'))); if(closeBtn){closeBtn.addEventListener('click',()=>{setMenuOpen(false); toggle.focus();});} overlay.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>setMenuOpen(false))); document.addEventListener('keydown',e=>{if(e.key==='Escape'&&overlay.classList.contains('active')){setMenuOpen(false); toggle.focus();}});} const progressBar=document.getElementById('scroll-progress'); if(progressBar){window.addEventListener('scroll',throttle(()=>{const docHeight=document.documentElement.scrollHeight-window.innerHeight; progressBar.style.width=(docHeight>0?(window.scrollY/docHeight)*100:0)+'%';},30));} const backTop=document.getElementById('back-to-top'); if(backTop){window.addEventListener('scroll',throttle(()=>backTop.classList.toggle('visible',window.scrollY>400),100)); backTop.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));}}
function normalizeFileName(value){if(!value)return 'index.html'; const clean=value.split('#')[0].split('?')[0].replace(/\/$/,''); return clean.split('/').pop()||'index.html';}
function setActiveLinks(){const currentFile=normalizeFileName(window.location.pathname); document.querySelectorAll('.navbar-links a,.nav-overlay a').forEach(link=>{const linkFile=normalizeFileName(link.getAttribute('href')); if(linkFile===currentFile){link.classList.add('active'); link.setAttribute('aria-current','page');}else{link.classList.remove('active'); link.removeAttribute('aria-current');}});}
